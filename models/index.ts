import { Thought } from "./Thought";

// If we have multiple data models, we can export all of them in a list as a convenience when providing the schema when opening a Realm.
export const schemas = [Thought];
